<UserControl x:Class="PDTUtils.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:PDTUtils.MVVM.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:AdminViewModel}" />
    </UserControl.Resources>
    
    <Grid>
        <TabControl>
            <TabItem Header="Game Settings" Width="200"></TabItem>
            <TabItem Header="Birth Cert" Width="200"></TabItem>
            <TabItem Header="System Settings" Width="200"></TabItem>
            <TabItem Header="Configure" Width="200"></TabItem> <!-- Hopper & note -->
            <TabItem Header="Smart Card" Width="200"></TabItem>
        </TabControl>
    </Grid>
</UserControl>
