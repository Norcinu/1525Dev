<UserControl x:Class="PDTUtils.Views.LampTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:PDTUtils.MVVM.ViewModels"
             xmlns:view="clr-namespace:PDTUtils.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    
    <Grid>
        <StackPanel HorizontalAlignment="Center">
            <Label Content="{Binding BannerMessage}" FontSize="35" Height="50" />
            <Button Content="Start" FontSize="35" Height="50" Width="125" Command="{Binding StartLampTest}" Visibility="{Binding ButtonVisibility}"/>
        </StackPanel>
    </Grid>
</UserControl>
