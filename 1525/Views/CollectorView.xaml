<UserControl x:Class="PDTUtils.Views.CollectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:PDTUtils.MVVM.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:CollectorViewModel}" />
    </UserControl.Resources>
    
    <Grid>
        <TabControl>
            <TabItem Header="Cash" Width="200"></TabItem>
            <TabItem Header="Empty" Width="200"></TabItem>
            <TabItem Header="Audit" Width="200"></TabItem>
        </TabControl>
    </Grid>
</UserControl>
